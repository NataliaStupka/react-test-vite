import s from './Header.module.css';

const Header = () => {
  return (
    <div>
      <header className={s.header}>
        <h3>Routing</h3>
      </header>
    </div>
  );
};

export default Header;
